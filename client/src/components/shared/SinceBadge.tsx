export function SinceBadge() {
  return (
    <span className="inline-flex items-center gap-1 rounded-full border border-brand-gold/50 bg-brand-gold-light px-3 py-1 text-xs font-semibold uppercase tracking-wider text-brand-maroon">
      <span aria-hidden>✦</span> Since 1998
    </span>
  );
}
